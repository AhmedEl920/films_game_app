import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Private constructor

  /// -------- Base Color --------
 static const Color primary = Color(0xFFFDC401);
static const Color secondary = Color(0xFF1E1E1E);
static const Color accent = Color(0xFFFFD54F);

static const Color background = Color(0xFFF5F5F5);
static const Color cardBackground = Colors.white;

static const Color textPrimary = Color(0xFF121212);
static const Color textSecondary = Color(0xFF6B7280);

static const Color divider = Color(0xFFE5E7EB);
static const Color white = Colors.white;
static const Color black = Colors.black;

static const Color success = Color(0xFF22C55E);
static const Color error = Color(0xFFEF4444);
  /// -------- Dark Theme Colors --------
  static const Color darkBackground = Color(0xFF0F0F0F);
static const Color darkCardBackground = Color(0xFF1A1A1A);

static const Color darkPrimary = Color(0xFFFDC401);

static const Color darkTextPrimary = Colors.white;
static const Color darkTextSecondary = Color(0xFFB3B3B3);

static const Color darkDivider = Color(0xFF2A2A2A);
}
